# 家庭小账本 Pro 运行稳定性与安全优化规范

更新日期：2026-03-16

## 1. 目标

- 长期运行下（数千到上万条记录）仍保持可用的滚动与渲染性能。
- 解决存储型 XSS 风险，保证导入与显示的安全性。
- 规避存储配额触顶导致的“假成功”与数据丢失。

## 2. 范围

- 单页静态应用（`index.html`）。
- 仅包含前端存储与渲染逻辑，无后端服务。

## 3. 关键变更概述

1. **存储层**：从 `localStorage` 迁移到 IndexedDB，支持更大容量，并申请持久化存储与用量估算。
2. **安全层**：用户输入与导入数据进行规范化与限制；输出统一做 HTML 编码，阻断存储型 XSS。
3. **性能层**：明细列表采用按日期分组的虚拟滚动，仅渲染可见窗口。

## 4. 数据模型

记录结构（Expense）：

- `id`：字符串，生成规则为 `Date.now()` + 随机串，最长 64 字符，`[a-zA-Z0-9_-]`。
- `date`：`YYYY-MM-DD`。
- `amount`：数字，必须大于 0。
- `category`：字符串，最长 20 字符。
- `isRigid`：布尔值。
- `desc`：字符串，最长 200 字符。

## 5. 存储设计

### 5.1 IndexedDB（优先）

- 数据库名：`family-account`
- 版本：`1`
- Store：`kv`
- Key：`familyExpenses`
- Value：`Expense[]`

### 5.2 迁移策略

- 首次启动若检测到 `localStorage` 中旧数据：
  - 解析并清洗为规范化结构。
  - 写入 IndexedDB 成功后删除旧 `localStorage`。
- IndexedDB 不可用时回退到 `localStorage`。

### 5.3 配额与持久化

- 使用 `navigator.storage.persist()` 申请持久化存储。
- 使用 `navigator.storage.estimate()` 估算配额与用量：
  - 用量超过 85% 时提示用户尽快备份与清理。
- 写入失败时必须回滚内存状态并告知用户。

## 6. 安全策略（XSS 防护）

### 6.1 输入规范化

- 去除控制字符（`\u0000-\u001F`、`\u007F`）。
- 统一 `trim`，限制长度。
- `id` 仅允许 `[a-zA-Z0-9_-]`。

### 6.2 输出编码

- 任何用户输入字段在渲染前必须做 HTML 编码。
- 涉及字段：`desc`、`category`、`date`。
- 禁止将用户输入直接拼接到 `innerHTML`。

### 6.3 导入清洗

- 导入 JSON 后统一走 `sanitizeExpense`。
- 无有效记录时提示并终止导入。

## 7. 性能策略（虚拟滚动）

### 7.1 分组与高度估算

- 按日期分组显示。
- 采用固定高度估算：
  - `RECORD_ITEM_HEIGHT = 72`
  - `RECORD_HEADER_HEIGHT = 28`
  - `RECORD_GROUP_MARGIN = 16`
  - `RECORD_OVERSCAN_PX = 320`

### 7.2 渲染策略

- 仅渲染“视口范围 + 缓冲区”的分组。
- 使用 `requestAnimationFrame` 节流滚动渲染。
- 分组与日合计全量计算，但 DOM 仅局部渲染。

### 7.3 已知限制

- 高度估算与实际高度存在偏差时可能出现轻微跳动。
- 如需更精准，后续可引入“测量 + 缓存行高”机制。

## 8. 验证清单

- [ ] 新增、编辑、删除记录能正常保存并刷新后保留。
- [ ] 导入含异常字段的 JSON 仍能稳定导入且不执行脚本。
- [ ] 数据量上万时滚动不卡顿。
- [ ] 存储接近上限时有提示；写入失败不出现“假成功”。

## 9. 风险与后续

- IndexedDB 配额由浏览器决定，仍可能触顶；需保留备份导出能力。
- 如需跨设备同步，必须引入后端或云存储方案。
- 虚拟滚动的高度估算可进一步改为动态测量以提升精度。
