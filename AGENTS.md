# Repository Guidelines

## 项目结构与模块组织

- 本仓库 是 单页 静态 应用，核心 代码 全部 位于 `index.html`。
- 业务 规范 与 安全 策略 记录 在 `spec.md`，请在 修改 存储 或 渲染 逻辑 前 先阅读。
- 维护 与 回归 清单 位于 `maintenance-checklist.md`，用于 月度 与 季度 检查。
- 依赖 以 CDN 方式 引入（Tailwind、FontAwesome、Chart.js），无 独立 assets 目录。

## 构建、测试 与 本地运行

- 无 构建 步骤；直接 打开 `index.html` 即可 运行。
- 可选 使用 静态 服务器：`python -m http.server 8000`，再访问 `http://localhost:8000`。
- 若 调试 存储 或 导入 功能，建议 使用 真实 浏览器 环境（非 文件 预览 模式）。

## 编码风格与命名约定

- 缩进 使用 4 空格；HTML、CSS、JS 保持 在 同一 文件，按 模块 分区 注释。
- 函数 与 变量 使用 `camelCase`；常量 使用 `UPPER_SNAKE_CASE`（如 `RECORD_ITEM_HEIGHT`）。
- DOM id 使用 `kebab-case`，与 现有 结构 保持 一致（如 `edit-modal`）。
- 避免 新增 依赖；如需 CDN 变更，固定 版本 并 在 维护清单 记录。

## 测试指南

- 当前 无 自动化 测试；以 手动 回归 为主。
- 变更 后 至少 验证：新增、编辑、删除、导入、导出、统计、打印。
- 大数据量 场景 需要 观察 滚动 性能 与 渲染 稳定性。

## 提交与 PR 规范

- 历史 提交 多为 简短 动词 + 对象，例如 `更新 index.html`、`修复长期运行可能存在的BUG`。
- 建议 保持 同样 风格，必要时 补充 影响 范围 或 模块 名称。
- PR 需要 描述 变更 动机、影响 面、手动 验证 结果。
- 涉及 UI 变化 时 提供 截图；涉及 存储 或 安全 变更 时 引用 `spec.md` 条目。

## 安全与配置提示

- 用户 输入 与 导入 数据 必须 经过 规范化 与 HTML 编码；禁止 直接 拼接 到 `innerHTML`。
- IndexedDB 为 优先 存储；写入 失败 必须 回滚 内存 状态 并 提示 用户。
- 当 存储 用量 接近 上限 时 提示 备份，备份 超过 30 天 需 预警。

## Agent 协作要求

- 与 我 交流 使用 简体中文；新增 文案 默认 使用 简体中文。
- 重要 修改 需 先 review，再 验证；验证 通过 后 再 汇报 成果。
- 若 变更 影响 规范 或 维护 流程，请 同步 更新 `spec.md` 或 `maintenance-checklist.md`。
